[pytest]
asyncio_mode = auto
testpaths = evals
pythonpath = lg .
markers =
    unit: fast offline eval checks without live graph execution
    integration: full graph eval pipeline with MCP and LLM services
